<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project3.gamja.mesPage.dao.HumanDAO">

	<select id="selectHuman" resultType="humanDTO" parameterType="humanDTO">
			SELECT e.emp_id, e.emp_name, p.po_name, d.dept_name, e.emp_hp, e.emp_add, e.emp_hiredate
				   FROM employee e
				   JOIN department d ON e.dept_id = d.dept_id
				   JOIN tbl_position p ON e.po_id = p.po_id
				   ORDER by emp_id
	</select>
		
</mapper>