<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project3.talking.gamja.dao.user.LibraryDAO">
	<select id="selectLibrary" resultType="LibraryDTO" parameterType="LibraryDTO">
			SELECT * from library
			WHERE lib_id = #{lib_id}
	</select>

</mapper>